# Info

Scripts in this directory are utilities for building and deploying the service as docker containers. Parts of the scripts should be replaced with solutions like 'fig' (http://www.fig.sh/) or similar. Nevertheless, the scripts are working fine for our current use case at the moment.
